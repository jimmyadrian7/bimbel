<html>
    
    <head>
        {% include "/Report/View/partial/style.twig" %}

        <style>
            header {
                position: fixed;
                top: 0px;
                left: 0;
                right: 0;
                height: 200px;
            }

            body {
                margin-top: 210px;
            }

            #background {
                background-image:url({{background}});
                display: block;
                position: fixed;
                top: 0;
                bottom: 0;
                left: 0;
                right: 0;
                z-index: -1000;
                background-repeat: repeat;
            }
        </style>
    </head>

    <body>
        
        <div id="background"></div>

        <div class="container">
            <header>
                <div class="container">
                    {% include "Report/View/partial/header.twig" %}
                </div>
            </header>
            
            <div class="content">
                <table class="table-content">
                    <thead>
                        <tr>
                            <th>No</th>
                            <th>Nama Guru</th>
                            <th>Jumlah</th>
                        </tr>
                    </thead>
                    {% for gaji in gajis %}
                        <tr>
                            <td>{{ loop.index }}</td>
                            <td>{{ gaji.guru.orang.nama }}</td>
                            <td>{{ gaji.komisi|number_format(0, ',', '.') }}</td>
                        </tr>
                    {% endfor %}
                    <tr class="bold">
                        <td class="bg-transparent" colspan="1"></td>
                        <td class="bg-red">Total</td>
                        <td class="bg-red">{{ total_gaji|number_format(0, ',', '.') }}</td>
                    </tr>
                </table>
            </div>
        </div>
    </body>
</html>